[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA fallback: serve index.html for client-side routes (React Router deep links).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
